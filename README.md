# To Do

The To Do app runs on Justine and stores its live tasks in `/home/ubuntu/data/todo.jsonl`. Authentication uses `/home/ubuntu/data/todo-auth.json`.

Neither file belongs in this public source repository. Back up the live data separately.
